import Route from '../../lib/route';
import HttpMethod from '../../lib/httpMethod';
import Auth from '../../controllers/auth';

const ARTICLE_URL = '/api/auth';

const auth: Route[] = [
];

export default auth;